#include <iostream>
#include <stdio.h>      /* printf */
#include <math.h>       /* sqrt */
#include <vector>

using namespace std;



int main ()
{
    vector<vector<int>> M(2, vector<int>(2)); cin >> M[0][0] >> M[0][1] >> M[1][0] >> M[1][1];
    int n, m;    cin >> n >> m;



}